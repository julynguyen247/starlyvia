server:
  port: 8082

spring:
  application:
    name: friend-service

  datasource:
    url: jdbc:postgresql://localhost:5434/friend_db
    username: starlyvia
    password: starlyvia
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
